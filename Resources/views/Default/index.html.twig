{% extends 'SiciarekPhotoGalleryBundle::public.layout.html.twig' %}
{% block content %}

    <h1 id="subtitle">{{ config.title | trans() }}</h1>

    <h3>{{ "homepage.body" | trans({}, "PhotoGalleryBundle") | raw }}</h3>

    <script type="text/javascript">
        // <![CDATA[

        var array = ["A", "B", "C", "D", "E", "F"];
        console.log(array);

        array.splice(3, 1);

        console.log(array);

        // ]]>
    </script>
{% endblock %}