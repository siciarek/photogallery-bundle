<script type="text/javascript">
    // <![CDATA[
    var translations = {
        {% for key, value in translations %}
        "{{ key }}" : "{{ key | trans() }}",
        {% endfor %}
        "{{ config.title }}" : "{{ config.title | trans() | raw }}"
    };

    /**
     * i18n javascript interface
     *
     * @param phrase
     * @return {*}
     */
    function __(phrase) {
        return typeof translations[phrase] !== "undefined" ? translations[phrase] : phrase;
    }

    // ]]>
</script>